# هيكلة المشروع — الرؤية والمعمارية

توثيق تقسيم المشروع على **4 مناطق** وكيف بنكبر فيهم على المدى البعيد، بالإضافة لقواعد المسارات
اللي لازم تلتزم بيها أي ملف جديد.

---

## 1) مناطق المشروع الأربع

المشروع بيتربي على **جزء واحد يضم كل المناطق**، وكل منطقة ليها ملفاتها وواجهتها الخاصة:

| المنطقة | المسار | الوظيفة |
|---|---|---|
| **منطقة المتابعة (الداخلية)** | `staff/` | كل صفحات إدارة السنتر: الحصة، الحضور، الطلاب، المتابعة، الامتحانات، المالية، الصندوق، الإعدادات، لوحة المعلم. |
| **منطقة ولي الأمر** | `parent/` | بوابة خارجية للعرض فقط: الخط الزمني، ملف الطالب، الحضور، المالية (عرض)، الدرجات والترتيب، المتابعة، التواصل. |
| **منطقة الطالب** | `student/` | منصة الطالب (الحالية: بوابة عرض ملفه الدراسي، والمستقبل: واجبات، مسابقات، امتحانات أونلاين، بنك أسئلة، ومتابعة أخطاء دقيقة). |
| **منطقة الويب سايت** | `site/` | المنصة العامة: الصفحة التسويقية + الدخول المشترك + فيديوهات الشرح + الحصص الأونلاين (حل واجبات / مراجعات امتحانات). |

**نقطة الدخول:** `index.html` في الجذر بتحول تلقائي حسب الدور:
- بدون تسجيل دخول → `site/` (الموقع هو بوابة الدخول للجميع)
- ولي أمر → `parent/`
- طالب → `student/`
- مدير / مساعد → `staff/dashboard.html`

**بعد تسجيل الدخول** (من `login.html`): الوجهة حسب الدور — طالب → `/student/` ، ولي أمر → `/parent/` ، إدارة/مدرّس → `/staff/dashboard.html`.

**تحكم المدرّس في البوابات** (من `staff/settings.html` → "🚪 بوابات الدخول"):
- `parentPortalEnabled` / `studentPortalEnabled` بيحددوا ظهور تبويب وأزرار الدخول لكل بوابة على الموقع وصفحة تسجيل الدخول،
- والبوابة نفسها (`parent-portal.js`) بتعمل تسجيل خروج وتحويل تلقائي لو البوابة متقفولة — فكمان لو الدخول جرب على الرابط مباشرة مش هيفتح.
- لو الاتنين متقفولين = النظام شغّال **متابعة داخلية فقط** بدون أي بوابات للطلاب/أولياء الأمور.

---

## 2) فكرة كل منطقة (الرؤية بعيدة المدى)

### أ. منطقة الطالب — منصة الطالب (`student/`)
- **الواجبات**: المدرس بينزّل واجب، والطالب يحل ويرفع إجابته.
- **المسابقات**: مسابقات بين الطلاب يديرها المدرس.
- **الامتحانات الأونلاين**: امتحانات يحلها الطالب من البيت ويتم تصحيحها.
- **بنك الأسئلة**: أسئلة مصنفة حسب الصف/المجموعة/الفكرة.
- **تحليل الأخطاء**: النظام بيمسك أخطاء الطالب بدقة (سؤال غلط، فكرة متكررة) ويعمل لكل طالب ملف أخطاء.
- **تقارير للمدرس**: تقرير تلقائي بـ **الأخطاء الشائعة** عبر الطلاب، عشان المدرس يعدل طريقة الشرح ويحل المشكلة المنتشرة.

### ب. منطقة ولي الأمر — بوابة متابعة الطفل (`parent/`)
- ولي الأمر يتابع **كل حركة من حركات ابنه**: حضور، غياب، درجات، تقارير متابعة، مالية (عرض فقط).
- **إشعارات**: إشعار بكل تحديث جديد (الحضور، النتيجة، ملاحظة متابعة، أي تصعيد).
- **الطالب نفسه** بيدخل من بوابته (`student/`) ويشوف ملفه الدراسي — كلها **عرض فقط بدون أي إجراءات**، وكلاهم بيستخدموا نفس الوحدة `parent-portal.js` ببراند مختلف حسب الدور.

### ج. منطقة المتابعة — الداخلية للسنتر (`staff/`)
- كل شغل اليومي: استقبال، حصص، حضور، امتحانات، مالية، صندوق، ترحيل.
- **لوحة المعلم** (`teacher-insights`) هي بذرة منطقة المتابعة الذكية: تصعيد الحالات، المتابعة الدورية، وتقارير الأخطاء الشائعة اللي هتغذي بيها لوحة المعلم من منصة الطالب.

### د. منطقة الويب سايت — المنصة العامة (`site/`)
- الوجهة العامة للجمهور: تعريف بالمدرس، مميزات، حجز، تواصل.
- المستقبل: **الحصص الأونلاين** — حل واجبات ومراجعات امتحانات عبر فيديوهات، أي سطحة شرح تانية غير السنتر (الحصة في السنتر = `staff/session`، والحصة أونلاين = `site`).

---

## 3) قواعد المسارات (مهمة لأي ملف جديد)

- **الأصول المشتركة ثابتة في الجذر** (`assets/`): CSS وJS وMock — مش بتتحرك مع المنطقة، عشان الـ imports تفضل شغالة.
- **HTML داخل منطقة** بيشاور على الأصول بـ `../assets/...` (نسبي لموقعه).
- **روابط بين صفحات نفس المنطقة**: نسبي مباشر `session.html` → `session.html`.
- **روابط بين مناطق مختلفة**: **root-absolute** → `/staff/...` ، `/parent/` ، `/login.html` ، `/site/` (الوضع الآمن لأي مكان).
- **التنقل داخل الـ JS** (اللي بيحصل من `window.location` أو `href` متولّد): بيستخدم **`appPath()`** من `assets/js/paths.js` بدل مسار جذري ثابت، لأن المسار الجذري بيكسر لو التطبيق متشغّل من مجلد فرعي (Live Server / GitHub Pages) — `appPath` بيبني الرابط من مكان ملف الـ JS نفسه فيشتغل من أي mount point:
  ```js
  import { appPath } from "./paths.js";
  window.location.href = appPath("staff/dashboard.html");
  window.location.href = appPath("parent/");
  window.location.href = appPath("login.html");
  ```
- **تحميل بيانات Mock**: `storage.js` بيحسب المسار من موضع الملف نفسه:
  ```js
  const MOCK_BASE = new URL("../assets/mock/", import.meta.url).href;
  ```
  فـ `fetch` تشتغل من أي صفحة في أي منطقة، ومن غير ما نعتمد على root الرفع.

> ملحوظة: الفكرة إن الـ HTML النسبي يعتمد على مكان الصفحة، والتنقل اللي من الـ JS يعتمد على `appPath()`، فمش لازم نعتمد على الرابط الجذري للدومين — وده بيخلي الرفع يشتغل من أي مجلد فرعي أو من جذر الدومين.

---

## 4) خريطة الملفات

```
/
├── index.html              دخول مشترك (تحويل حسب الدور)
├── login.html              تسجيل الدخول (وجهة بعد الدخول حسب الدور)
├── 404.html
├── parent/
│   └── index.html          بوابة ولي الأمر (parent-portal.js)
├── student/
│   └── index.html          بوابة الطالب (parent-portal.js) — عرض ملفه الدراسي
├── site/
│   └── index.html          الويب سايت (المنصة العامة)
├── staff/                  كل صفحات الإدارة الداخلية
│   ├── dashboard.html  session.html  visit.html  quick-attendance.html
│   ├── students.html   student.html  student-form.html  group-students.html
│   ├── followup.html   teacher-insights.html  attendance-tracker.html
│   ├── exams.html  finance.html  shift.html  rollover.html  settings.html
│   └── reception.html / parent-reception.html  (تحويلات قديمة → visit.html)
├── assets/
│   ├── css/                style.css (Design System) + parent.css
│   ├── js/                 وحدات ES6 مشتركة + منطق كل صفحة
│   └── mock/               بيانات تجريبية
├── scripts/
│   ├── tests/              test-scenarios / test-output / test-data-injection
│   └── generate-test-data.js
└── shared/                 ملفات قديمة (auth.js / sync.js) — مرجعية
```

---

## 5) مخطط النمو المقترح (Roadmap)

1. **تثبيت منصة الطالب** داخل `student/` (واجبات + بنك أسئلة + امتحانات أونلاين) مع ملتقى البيانات في `assets/js` نفسه.
2. **محرك تحليل الأخطاء**: تسجيل أخطاء من الامتحانات/الواجبات → ملف أخطاء لكل طالب → تقرير أخطاء شائعة.
3. **ربط المتابعة**: لوحة المعلم تعرض الأخطاء الشائعة، وولي الأمر يتلقى إشعارات من أي تحرك.
4. **الويب سايت الكامل** داخل `site/`: فيديوهات وحصص أونلاين مرتبطة بنفس بيانات الطلاب (الحضور الأونلاين يندمج مع الشهري).
